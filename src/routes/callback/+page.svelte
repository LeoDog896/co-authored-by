<script lang="ts">
    import { onMount } from "svelte";

    onMount(() => {
        // get the query from the window url
        const query = window.location.search.substring(1);

        // 
    })
</script>